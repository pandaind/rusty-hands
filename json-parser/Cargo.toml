[package]
name = "json-parser"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] } # use serde for serialization and deserialization
serde_json = "1.0" # use serde_json for working with JSON data